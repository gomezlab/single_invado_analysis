Sample data is located on Balder, outside the svn repository in this directory:

/Users/Shared/mbergins/data_store/focal_adhesion_data

Most of the script defaults (which can be changed as needed) assume that the data lives in a 'data' directory.
