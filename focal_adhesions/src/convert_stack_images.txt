setBatchMode(true);
image_number = nImages;
for (i = 1; i <= image_number; i++) {
	home_directory = getDirectory("image");
	image_name = getTitle();
	saveAs("tif",home_directory+image_name);
	close();
}
